@*@using Microsoft.AspNet.Identity;*@
@model IEnumerable<mboard.Models.Board>

@{
    ViewBag.Title = "Tablice";
}

<h2>Moje</h2>
<h4>Tablice</h4>

<hr />
<button class="btn btn-success" onclick="location.href='@Url.Action("Create", "Test")'">
    Nowa Tablica
</button>
<hr />
<p>
    @using (Html.BeginForm("Index", "Test"))
    {
    <p>
        Nazwa: @Html.TextBox("SearchString")
        <input class="btn btn-primary btn-sm" type="submit" value="Szukaj" />
    </p>
    }
    @*@ViewBag.Message;*@
    @* @Html.ActionLink("Create New", "Create")*@

    <div class="board-container">
        @foreach (var item in Model)
        {
            <div class="btn btn-default board-thumb">
                @Html.DisplayFor(modelItem => item.Title)

                @Html.ActionLink("Edit", "Edit", new { id = item.Id })
            </div>
        }
    </div>
    <hr />
    <p>
        @{Html.RenderAction("Index", "Tag");}
    </p>


    @*<table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Title)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Color)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Height)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Width)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Last_mod)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Created)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.List_position)
                </th>
                <th></th>
            </tr>

        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Color)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Height)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Width)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Last_mod)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Created)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.List_position)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.Id })
                </td>
            </tr>
        }

        </table>*@
